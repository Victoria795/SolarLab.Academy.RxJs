import { BehaviorSubject, Subject } from "rxjs";
import { ajax } from "rxjs/ajax";

//subject

// const subject$ = new Subject<string>();

// subject$.next("FirstValue");

// subject$.subscribe((value: string) => console.log(value));

// subject$.next("SecondValue");
// subject$.next("ThirdValue");

//behaviourSubject

// const behaviourSubject$ = new BehaviorSubject<string>("InitialValue");

// behaviourSubject$.next("FirstValue");

// behaviourSubject$.subscribe((value: string) => console.log(value));

// behaviourSubject$.next("SecondValue");
// behaviourSubject$.next("ThirdValue");
